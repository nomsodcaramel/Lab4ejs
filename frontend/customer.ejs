<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Customer Information</title>
</head>

<body>
    <h2>Welcome <%= cInfo.cgender === 'Male' ? 'Mr.' : 'Miss' %></h2>
    <% for (let i = 0; i < (cInfo.cgender === 'Male' ? 5 : 10); i++) { %>
        <%= cInfo.cname %>
    <% } %>

    <h3>Your ID is <%= cInfo.cid %></h3>
    <p>zip code: <b><i><%= cInfo.czip %></i></b></p>

    <% var locationType = cInfo.ccity === 'Bangkok' ? 'capital' : 'province'; %>
    <%= cInfo.ccity %> <%= locationType %>
</body>

</html>
